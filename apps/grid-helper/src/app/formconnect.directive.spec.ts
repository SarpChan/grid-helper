import { FormconnectDirective } from './formconnect.directive';

describe('FormconnectDirective', () => {
  it('should create an instance', () => {
    const directive = new FormconnectDirective();
    expect(directive).toBeTruthy();
  });
});
